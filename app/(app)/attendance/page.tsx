'use client';

import { CalendarCheck2, Check, ChevronLeft, ChevronRight, Clock3, House, Plus } from 'lucide-react';
import { format, getMonth, getYear } from 'date-fns';
import { useState } from 'react';
import { toast } from 'sonner';
import { useMutation, useQueryClient } from '@tanstack/react-query';
import { api } from '@/lib/api';
import { useAttendance, useAttendanceSummary } from '@/lib/queries';
import { AttendanceCalendar, AttendanceList } from '@/components/attendance-calendar';
import { Button, Card, ErrorState, FormField, Input, LoadingSkeleton, PageHeader, Select, StatCard } from '@/components/ui';

export default function EmployeeAttendancePage() { const [month, setMonth] = useState(new Date()); const params = new URLSearchParams({ dateFrom: format(new Date(getYear(month), getMonth(month), 1), 'yyyy-MM-dd'), dateTo: format(new Date(getYear(month), getMonth(month) + 1, 0), 'yyyy-MM-dd'), limit: '100' }); const records = useAttendance(params); const summary = useAttendanceSummary(); const client = useQueryClient(); const [showForm, setShowForm] = useState(false); const mark = useMutation({ mutationFn: api.createAttendance, onSuccess: () => { toast.success('Attendance marked'); client.invalidateQueries({ queryKey: ['attendance'] }); setShowForm(false); }, onError: (error) => toast.error(error.message) });
  if (records.isLoading || summary.isLoading) return <LoadingSkeleton rows={8} />; if (records.isError) return <ErrorState message={records.error.message} retry={() => records.refetch()} />; const data = records.data?.items ?? []; return <div><PageHeader eyebrow="My time" title="Attendance" description="Keep an eye on your rhythm and make every workday count." action={<Button onClick={() => setShowForm((value) => !value)}><Plus className="h-4 w-4" />Mark attendance</Button>} /><div className="mb-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><StatCard label="Present" value={summary.data?.present ?? 0} hint="Days this month" icon={<CalendarCheck2 className="h-5 w-5" />} accent="coral" /><StatCard label="Late arrivals" value={summary.data?.late ?? 0} hint="Needs your attention" icon={<Clock3 className="h-5 w-5" />} accent="coral" /><StatCard label="On leave" value={summary.data?.leave ?? 0} hint="Approved time off" icon={<House className="h-5 w-5" />} accent="lavender" /><StatCard label="Attendance rate" value={`${summary.data?.attendancePercentage ?? 0}%`} hint="Working days recorded" icon={<Check className="h-5 w-5" />} accent="lime" /></div>{showForm && <Card className="mb-6 border-[#ff725c]/30 bg-[#fffaf1] p-5"><div className="mb-4"><h2 className="font-display text-xl">Mark a workday</h2><p className="text-xs text-[#85878d]">Use this for a missed punch or a remote day.</p></div><form className="grid gap-4 sm:grid-cols-3" onSubmit={(event) => { event.preventDefault(); const form = new FormData(event.currentTarget); mark.mutate({ date: String(form.get('date')), status: String(form.get('status')), checkIn: `${String(form.get('date'))}T09:00:00`, checkOut: `${String(form.get('date'))}T18:00:00`, workHours: 8 }); }}><FormField label="Date"><Input name="date" type="date" required defaultValue={format(new Date(), 'yyyy-MM-dd')} /></FormField><FormField label="Status"><Select name="status" defaultValue="PRESENT"><option value="PRESENT">Present</option><option value="WORK_FROM_HOME">Work from home</option><option value="LEAVE">Leave</option></Select></FormField><div className="flex items-end"><Button type="submit" disabled={mark.isPending} className="w-full">{mark.isPending ? 'Saving…' : 'Save attendance'}</Button></div></form></Card>}<div className="grid gap-5 xl:grid-cols-[1.1fr_0.9fr]"><Card className="p-5 sm:p-6"><div className="mb-5 flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.13em] text-[#ff725c]">Monthly view</p><h2 className="mt-1 font-display text-xl">{format(month, 'MMMM yyyy')}</h2></div><div className="flex gap-1"><button aria-label="Previous month" className="flex h-9 w-9 items-center justify-center rounded-xl border border-[#e8e4dc] bg-white hover:bg-[#fffaf1]" onClick={() => setMonth(new Date(getYear(month), getMonth(month) - 1, 1))}><ChevronLeft className="h-4 w-4" /></button><button aria-label="Next month" className="flex h-9 w-9 items-center justify-center rounded-xl border border-[#e8e4dc] bg-white hover:bg-[#fffaf1]" onClick={() => setMonth(new Date(getYear(month), getMonth(month) + 1, 1))}><ChevronRight className="h-4 w-4" /></button></div></div><AttendanceCalendar records={data} month={month} /></Card><Card className="p-5 sm:p-6"><div className="mb-5"><p className="text-xs font-bold uppercase tracking-[0.13em] text-[#ff725c]">Timeline</p><h2 className="mt-1 font-display text-xl">Recent records</h2></div><AttendanceList records={data.slice(0, 8)} /></Card></div></div>; }
